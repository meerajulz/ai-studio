/**
 * GoogleTrainer (Milestone 23) — registered but DISABLED. Placeholder in the Training Registry.
 */
import { stubTrainer } from "./stub";

export const googleTrainer = stubTrainer({
  id: "google",
  label: "Google",
  supports: [],
  enabled: false,
  priority: 65,
});
