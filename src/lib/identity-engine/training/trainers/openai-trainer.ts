/**
 * OpenAITrainer (Milestone 23) — registered but DISABLED. Placeholder in the Training Registry.
 */
import { stubTrainer } from "./stub";

export const openAiTrainer = stubTrainer({
  id: "openai",
  label: "OpenAI",
  supports: [],
  enabled: false,
  priority: 70,
});
